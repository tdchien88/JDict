var code = "1";
var x = `{
code: "c`+code+`",
name: "bai `+code+`",
cotent: [`; 
x += "`";
for(var i=0; i< j$('.boxtv').length; i++)
{ 
	x += j$(j$(".boxtv")[i]).context.outerHTML; 
	x += '<div class="newline"></div>`,`'
}
x += "`]";
x += `
},`;